spring:
  application:
    name: staff-service
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      server-addr: 8.130.69.82:8848 # nacos地址
      #server-addr: nacos:8848
      config:
        file-extension: yaml # 文件后缀名
